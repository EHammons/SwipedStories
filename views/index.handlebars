<div class="container">
    <img src="swiper.png" id="titleImg">
    <div class="title">
        <h1>Swiped Stories</h1>
        <h3>All the News that's fit to Swipe</h3>
        <button class="swipeNews">Swiper, Swipe!</button>
    </div>
</div>

<div class="container">
    <ul class="list-group">
        {{!-- {{#each dbArticle}} --}}
            {{> article-block}}
        {{!-- {{/each}} --}}
    </ul>
</div>